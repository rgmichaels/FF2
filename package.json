{
  "name": "ff2",
  "version": "0.1.0",
  "private": true,
  "description": "Intent-driven QA automation platform",
  "scripts": {
    "dev:worker": "tsx apps/worker/src/index.ts",
    "db:generate": "prisma generate --schema packages/db/prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema packages/db/prisma/schema.prisma",
    "db:seed": "tsx packages/db/prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prisma": "^6.12.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
